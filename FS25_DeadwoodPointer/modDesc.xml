<?xml version="1.0" encoding="utf-8" standalone="no"?>
<modDesc descVersion="108">
    <author>m.p.tetlow</author>
    <version>1.1.1.0</version>

    <title>
        <en><![CDATA[Deadwood Tree Pointer]]></en>
    </title>

    <description>
        <en><![CDATA[
Marks the deadwood trees that an active contract actually requires.

Deadwood contracts give you a circle on the map, and in the world only a small sprayed mark on each
trunk that you cannot see until you are already standing at the tree. This mod fills the gap between
the two: every tree the contract counts gets a marker above it, visible across the stand, and trees
that are off-screen get an arrow at the edge of the screen pointing the way. A readout shows how many
are left and how far the nearest one is.

Only trees belonging to a deadwood contract you are actually running are marked, and a tree's marker
disappears the moment you finish the cut. Trees that merely look dead, and normal trees standing among
them, are ignored — the tree list comes from the contract itself, not guessed from the tree. Wood
transport contracts are never marked, whether their logs are on the ground or already on your trailer,
so you can run one alongside a deadwood contract and still only see the trees you have to cut.

Multiplayer supported: markers are drawn locally for each player, and everyone in the farm that holds
the contract sees them. Nothing is sent over the network.

Toggle with F7, rebindable in Options > Controls.
        ]]></en>
    </description>

    <iconFilename>icon_DeadwoodPointer.dds</iconFilename>

    <multiplayer supported="true" />

    <l10n filenamePrefix="translations/translation" />

    <extraSourceFiles>
        <sourceFile filename="scripts/DeadwoodPointer.lua" />
    </extraSourceFiles>

    <actions>
        <action name="DWPOINTER_TOGGLE" category="ONFOOT VEHICLE"
                axisType="HALF" ignoreComboMask="false" />
    </actions>

    <!--
        The default key MUST live here, NOT as a nested binding element inside the action element.
        mods.lua registers the two on separate schema paths: modDesc.actions.action(?) carries only
        attributes (mods.lua:1118-1122), while the key is
        modDesc.inputBinding.actionBinding(?).binding(?) (mods.lua:1123-1129).

        XML outside the schema is discarded in silence, so nesting the binding inside the action
        looks right, validates, loads, and registers the action, then simply ships with no key
        attached. Every version up to 1.1.0.0 did exactly that: the action appeared in
        Options / Controls under the mod's name with nothing bound to it.

        The key is F7, not Ctrl+D as originally intended. KEY_lctrl KEY_d is already the vanilla
        AXIS_CRANE_SUPPORT binding, and lctrl on its own is crouch. F7 takes no modifier, so no
        combo mask applies to it.

        A key already in the player's own inputBinding.xml always wins over this default, so anyone
        who bound the action by hand while it shipped unbound keeps their key.
    -->
    <inputBinding>
        <actionBinding action="DWPOINTER_TOGGLE">
            <binding device="KB_MOUSE_DEFAULT" input="KEY_f7" />
        </actionBinding>
    </inputBinding>
</modDesc>

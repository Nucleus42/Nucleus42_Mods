<?xml version="1.0" encoding="utf-8" standalone="no"?>
<modDesc descVersion="106">
    <author>Shadow</author>
    <version>1.0.0.0</version>

    <title>
        <en><![CDATA[Subsoiler Tillage]]></en>
    </title>

    <description>
        <en><![CDATA[Lets subsoilers do the plough's job.

Any implement in the "Subsoilers" store category gains the three things that
previously only a plough could do:

  - Create new fields (toggle with the same key a plough uses)
  - Leave PLOUGHED ground state, clearing the "needs ploughing" warning
  - Count towards ploughing contracts

Nothing else changes. The game still decides when a field needs ploughing;
this mod only adds another implement that can satisfy it.

Note: an affected subsoiler now leaves ploughed ground instead of cultivated
ground, so it no longer completes cultivating contracts.]]></en>
    </description>

    <iconFilename>icon.png</iconFilename>

    <multiplayer supported="true"/>

    <specializations>
        <specialization name="subsoilerPlow" className="SubsoilerPlow" filename="scripts/SubsoilerPlow.lua"/>
    </specializations>

    <extraSourceFiles>
        <sourceFile filename="scripts/events/SubsoilerLimitToFieldEvent.lua"/>
        <sourceFile filename="scripts/SubsoilerTillage.lua"/>
    </extraSourceFiles>
</modDesc>
